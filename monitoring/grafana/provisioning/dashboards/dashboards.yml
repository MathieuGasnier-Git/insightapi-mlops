apiVersion: 1

providers:
  - name: InsightAPI
    type: file
    folder: InsightAPI
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
