# InsightAPI MLOps

Monorepo for the InsightAPI platform.

## Structure

- `frontend/` — web client
- `backend-main/` — primary backend API
- `ml-service/` — machine learning service
- `service-2/` — additional backend service
